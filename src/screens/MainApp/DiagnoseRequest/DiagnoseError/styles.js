import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  errorContainer: {
    flex: 0.05
  },
  errorMessage: {
    position: 'absolute',
    alignSelf: 'center'
  }
})

export default styles
